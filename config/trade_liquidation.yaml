# ============================================================
# Trade Platform — конфиг трейдера trade_liquidation
# Версия: переписанный и прокомментированный вариант
# ============================================================

# Как часто перезапускать цикл трейдера (в минутах).
# 0.2 = примерно 12 секунд.
restart_interval_minutes: 0.2

traders:
  - name: trade_liquidation            # Имя трейдера (используется в логах и идентификации)
    enabled: true                      # true = трейдер включен, false = выключен
    version: "1.0"                     # Версия логики трейдера (информативно)

    params:
      # ======================================================
      # Идентификация биржи / аккаунта
      # ======================================================

      exchange_id: 1                   # ID биржи в БД (таблица exchanges)
      account_name: base_main          # Человеко-читаемое имя аккаунта
      account_id: 1                    # Внутренний ID аккаунта

      # ======================================================
      # Источник сигналов (скринеры) и фильтры по сигналам
      # ======================================================

      # Какие скринеры используются как источник сигналов.
      # Можно указывать список имен из таблицы screeners.name
      screener_name: ["scr_liquidation_binance", "scr_oi_binance","scr_pump_binance"]

      signal_status_new: NEW           # Какой статус сигнала считать "новым"

      # Разрешённые таймфреймы сигналов
      allowed_timeframes: ["5m", "15m", "1h"]

      # Максимальный возраст сигнала в минутах от signal_ts
      max_signal_age_minutes: 120

      # Кулдаун по символу:
      # не открывать новую сделку по тому же символу, если недавно уже была сделка/попытка
      per_symbol_cooldown_minutes: 120

      # Максимум попыток открытия сделки по одному сигналу
      max_attempts_per_signal: 3

      # Пауза между повторными попытками по одному и тому же сигналу
      retry_cooldown_minutes: 2

      # Ключ в context/jsonb для хранения счётчика попыток
      attempts_context_key: trade_liquidation

      # Если число попыток превышено — переводить сигнал в expire_status
      expire_on_attempts_exceeded: true

      # Статус сигнала при экспирации
      expire_status: EXPIRED

      # Автоматически экспирировать старые NEW-сигналы
      expire_old_signals: true

      # Сигнал NEW экспирируется, если старше этого возраста
      expire_max_age_minutes: 120

      # Максимум сигналов за один проход блока экспирации
      expire_batch_limit: 5000

      # ======================================================
      # Режим работы
      # ======================================================

      mode: live                       # live = реальные ордера; paper/sim обычно используются для тестов, если поддерживаются кодом

      # ======================================================
      # Binance API / сеть
      # ======================================================

      api_key_env: BINANCE_BASE_MAIN_API_KEY        # Имя переменной окружения с API KEY
      api_secret_env: BINANCE_BASE_MAIN_API_SECRET  # Имя переменной окружения с API SECRET

      base_url: https://fapi.binance.com            # Базовый URL Binance Futures API
      recv_window_ms: 5000                          # recvWindow для signed-запросов
      timeout_sec: 10                               # Общий таймаут HTTP-запроса

      # Источник баланса:
      # true = брать напрямую с биржи
      # false = брать из БД
      use_exchange_balance: false

      binance_connect_timeout_sec: 3.0              # Таймаут установления TCP/HTTP соединения
      binance_read_timeout_sec: 15.0                # Таймаут ожидания ответа
      binance_max_retries: 3                        # Число сетевых ретраев
      binance_retry_backoff_sec: 1.0                # Пауза между ретраями

      # ======================================================
      # Получение и кэширование баланса
      # ======================================================

      wallet_balance_retries: 3                     # Сколько раз повторять попытку чтения баланса
      wallet_balance_retry_backoff_sec: 3.0         # Пауза между ретраями баланса

      wallet_balance_cache_ttl_sec: 180             # TTL кэша баланса
      wallet_balance_poll_sec: 10                   # Частота опроса баланса

      # ======================================================
      # Риск и ограничения на позиции
      # ======================================================

      leverage: 20                                  # Плечо
      margin_type: CROSSED                          # Тип маржи: CROSSED / ISOLATED
      hedge_enabled: true                           # true = hedge mode с LONG/SHORT

      # --- Hedge sizing с учетом funding
      hedge_funding_kof_limit: 0.6                  # Порог funding в процентах, после которого разрешается корректировка размера hedge
      hedge_koff_funding_boost: 1.5                 # Усиление hedge_koff при выгодном funding
      hedge_koff: 1.0                               # Множитель размера hedge относительно main

      # --- EMA-логика для hedge
      hedge_ema_window: 5                           # Период EMA
      hedge_ema_interval: "5m"                      # Таймфрейм свечей для EMA
      hedge_ema_confirm_bars: 2                     # Сколько баров нужно для подтверждения направления
      hedge_ema_min_slope_pct: 0.08                 # Минимальный slope EMA, чтобы считать направление валидным
      hedge_ema_flip_slope_mult: 0.7                # Смягчение порога slope для закрытия по EMA flip
      hedge_cooldown_sec: 180                       # Кулдаун между действиями по hedge
      hedge_close_on_level: true                    # Закрывать hedge возле значимого уровня
      hedge_close_on_ema_flip: true                 # Закрывать hedge при перевороте EMA
      hedge_level_tolerance_pct: 0.05               # Допуск вокруг уровня в процентах
      hedge_reopen_enabled: false                   # Повторно открывать hedge после его закрытия
      hedge_reopen_drawdown_pct: 0.5                # Порог drawdown main-позиции для повторного открытия hedge

      hedge_reopen_price_filter_enabled: true       # Включить повторный перезаход по цене относительно прошлого выхода hedge
      hedge_reopen_price_move_pct: 0.3              # Для LONG: цена должна стать выше anchor на 0.3%; для SHORT: ниже на 0.3%
      hedge_reopen_anchor_shift_trigger_pct: 1.5    # Сдвигать anchor, если цена ушла на этот % в сторону основной позиции
      hedge_reopen_anchor_shift_step_pct: 1.0       # На сколько сдвигать anchor при срабатывании триггера

      # ======================================================
      # Hedge close at entry (break-even after profit activation)
      # ======================================================

      hedge_close_on_entry_enabled: true              # Закрывать hedge около цены входа после того, как он уже был в прибыли и текущий P/L hedge >= 0
      hedge_close_on_entry_profit_activation_pct: 0.5 # Сначала hedge должен уйти в прибыль минимум на 0.5% от своей цены входа
      hedge_close_on_entry_buffer_pct: 0.35           # Буфер около цены входа для закрытия hedge в безубыток/около безубытка, но только при P/L hedge >= 0

      # Если main закрылась, а hedge остался открыт —
      # разрешить дополнительное сопровождение hedge
      hedge_add_on_main_close_enabled: true         # Разрешить сопровождать и при необходимости добирать hedge после закрытия основной позиции
      hedge_add_on_main_close_max_adds: 2           # Максимум дополнительных доборов hedge после закрытия main

      all_P_L: 5.0                                  # Закрыть обе позиции, если суммарный unrealized PnL достиг этого значения в USDT

      # ======================================================
      # Hedge trailing
      # ======================================================

      hedge_trailing_enabled: true                  # Включить отдельный trailing для hedge
      hedge_trailing_activation_pct: 1.5            # Активация trailing в % от средней цены входа hedge
      hedge_trailing_trail_pct: 0.5                 # callbackRate (%) для trailing
      hedge_trailing_activation_buffer_pct: 0.3     # Буфер от мгновенного срабатывания
      hedge_trailing_place_retries: 3               # Ретраи постановки hedge trailing
      hedge_trailing_working_type: MARK_PRICE       # MARK_PRICE или CONTRACT_PRICE
      hedge_trailing_reissue_on_avg_entry_change: true  # Перевыставлять hedge trailing при изменении средней цены hedge

      hedge_trailing_reissue_on_qty_change: true    # Перевыставлять hedge trailing при изменении размера hedge-позиции
      hedge_trailing_reissue_cooldown_sec: 30       # Минимальная пауза между повторными перевыставлениями hedge trailing
      hedge_trailing_qty_tolerance: 1e-8            # Допуск сравнения qty, ниже которого изменение считается шумом

      # ======================================================
      # Hedge take-profit
      # ======================================================

      hedge_take_profit_enabled: true               # Включить отдельный take-profit для hedge
      hedge_take_profit_pct: 2.0                    # Процент тейк-профита hedge относительно средней цены входа hedge
      hedge_take_profit_working_type: MARK_PRICE    # Тип триггерной цены для hedge TP: MARK_PRICE или CONTRACT_PRICE

      # ======================================================
      # Hedge stop-loss
      # ======================================================

      hedge_stop_loss_working_type: MARK_PRICE      # Тип триггерной цены для hedge stop-loss
      hedge_stop_loss_place_retries: 3              # Сколько раз пытаться поставить hedge stop-loss при ошибке
      hedge_stop_loss_reissue_cooldown_sec: 30      # Минимальная пауза между перевыставлениями hedge stop-loss
      hedge_stop_loss_qty_tolerance: 1e-8           # Допуск сравнения qty для hedge stop-loss

      # 0 = выключено
      # SHORT: stop = entry * (1 + pct/100)
      # LONG : stop = entry * (1 - pct/100)
      hedge_stop_loss_pct: 0.0                      # Базовый стоп-лосс hedge в процентах от цены входа

      # ======================================================
      # Hedge SL move to Break-Even / Profit-Lock
      # Работает только если hedge_stop_loss_pct > 0
      # ======================================================

      hedge_stop_loss_be_pct: 0.6                   # Куда переносить hedge SL после активации: процент прибыли/запаса относительно entry
      hedge_stop_loss_be_activation_pct: 0.90       # При каком движении в прибыль активировать перенос hedge SL в BE / profit-lock

      # Ограничения параметров BE
      hedge_stop_loss_be_pct_min: 0.5               # Минимально допустимое значение hedge_stop_loss_be_pct
      hedge_stop_loss_be_pct_max: 5.0               # Максимально допустимое значение hedge_stop_loss_be_pct
      hedge_stop_loss_be_activation_pct_min: 0.5    # Минимально допустимое значение hedge_stop_loss_be_activation_pct
      hedge_stop_loss_be_activation_pct_max: 8.0    # Максимально допустимое значение hedge_stop_loss_be_activation_pct

      # Логика сопровождения hedge после последнего добора main
      hedge_after_last_add_follow_enabled: false          # Включить отдельное сопровождение hedge после последнего добора основной позиции
      hedge_close_on_main_positive: false                 # Закрывать hedge, если основная позиция вышла в плюс

      hedge_after_last_add_follow_distance_pct: 2.0       # Дистанция сопровождения hedge после последнего добора main
      hedge_after_last_add_follow_pullback_pct: 1.5       # Допустимый откат от экстремума при сопровождении hedge
      hedge_after_last_add_follow_cooldown_sec: 120       # Кулдаун между действиями follow-логики hedge
      hedge_after_last_add_follow_min_trigger_move_pct: 0.8 # Минимальное движение цены, после которого включается follow-логика

      hedge_after_last_add_cancel_when_main_profit: false  # Отменять это сопровождение hedge, если main уже вышла в прибыль

      # ======================================================
      # Лимиты по позициям
      # ======================================================

      max_open_positions: 5                        # Максимум одновременно открытых позиций
      max_position_notional_pct_wallet: 15.0       # Ограничение notional одной позиции как % от кошелька
      risk_wallet_pct: 2.0                         # Риск на сделку от кошелька

      live_entry_reduce_on_max_position: true
      live_entry_reduce_steps: 5
      live_entry_reduce_factor: 0.85

      # ======================================================
      # Базовые SL / TP
      # ======================================================

      stop_loss_pct: 2.0                           # Базовый SL (%)
      take_profit_pct: 6.0                         # Базовый TP (%)
      recalc_sl_tp: true                           # Пересчитывать SL/TP после усреднений

      # ======================================================
      # Вход и bracket-ордера
      # ======================================================

      entry_order_type: market                     # Тип входа: market / limit
      sl_order_mode: stop_market                   # Режим постановки SL
#     sl_order_mode: trailing_stop_market          # Альтернативный режим SL
      tp_order_mode: take_profit_market            # Режим постановки TP

      reduce_only: true                            # reduceOnly для защитных/закрывающих ордеров
      working_type: MARK_PRICE                     # Тип триггерной цены для SL/TP/условных ордеров
      time_in_force: GTC                           # Политика времени жизни ордера: GTC / IOC / FOK
      client_order_id_prefix: TL                   # Префикс clientOrderId
      enable_stop_loss: true                       # Включить постановку SL
      enable_take_profit: true                     # Включить постановку TP

      defer_stop_loss_until_last_add: true         # Не ставить SL сразу, пока не будут выполнены все доборы
      sl_after_last_add_distance_pct: 2.0          # Дистанция SL после последнего добора, если включён отложенный SL

      abort_on_bracket_fail: true                  # Прерывать обработку позиции, если bracket-ордера не удалось выставить
      close_on_bracket_fail: true                  # Принудительно закрывать позицию, если bracket не удалось выставить
      signal_status_on_bracket_fail: CANCELLED     # В какой статус переводить сигнал при неуспешной постановке bracket

      # ======================================================
      # Trailing stop для main
      # ======================================================

      trailing_enabled: true                       # Включить trailing
      trailing_activation_pct: 1.5                 # Активация trailing после +X% в прибыль
      trailing_trail_pct: 0.5                      # Дистанция trailing (%)

      trailing_activation_buffer_pct: 0.5         # Буфер от мгновенного срабатывания trailing после его постановки
      tp_trigger_buffer_pct: 0.5                  # Буфер для triggerPrice TP, чтобы ордер не срабатывал мгновенно

      trailing_place_retries: 3                    # Сколько раз повторять постановку trailing при ошибке Binance
      tp_place_retries: 3                          # Сколько раз повторять постановку TP при ошибке Binance

      # ======================================================
      # Усреднение (averaging / adds)
      # ======================================================

      averaging_enabled: true                      # Разрешить доборы
      averaging_max_adds: 2                        # Максимум доборов
      add_or_hedge_invariant_enabled: true         # Гарантировать: если нет добора, то должен быть hedge

#     averaging_add_size_mode: risk_pct            # Альтернативный режим расчета размера добора
#     averaging_add_size_value: 10                 # Значение для альтернативного режима
      averaging_add_size_mode: position_mult       # Режим добора: увеличить позицию в N раз
      averaging_add_position_multiplier: 2.0       # Во сколько раз увеличивать позицию при ADD1

      averaging_order_type: market                 # Тип ордера добора: market / limit, если поддержано кодом

      # Построение уровней для доборов
      averaging_levels_tf: 4h                      # Таймфрейм, по которому строятся уровни для усреднения
      averaging_levels_lookback_hours: 480         # Глубина истории в часах для поиска уровней добора
      averaging_levels_method: combined            # Метод поиска уровней: например vp/pivot/combined в зависимости от реализации
      averaging_level_tolerance_pct: 0.15          # Допуск вокруг найденного уровня, при котором он считается пригодным

      averaging_min_level_distance_pct: 5.0        # Минимальная дистанция от текущей/входной цены до уровня добора

      # Параметры volume profile
      averaging_vp_bins: 48                        # Количество корзин (bins) для volume profile
      averaging_vp_top_nodes: 10                   # Сколько top volume nodes учитывать при выборе уровней

      # Параметры pivot / кластеризации
      averaging_pivot_left: 5                      # Количество свечей слева для определения pivot
      averaging_pivot_right: 5                     # Количество свечей справа для определения pivot
      averaging_cluster_tolerance_pct: 0.25        # Допуск для объединения близких уровней в кластер

      # Технические параметры перевыставления/сравнения уровней
      replace_cooldown_sec: 60                     # Минимальная пауза между cancel/replace ордеров
      avg_eps_pct: 0.02                            # Допуск сравнения средних цен/уровней в процентах
      price_eps_ticks: 2                           # Допуск сравнения цены в тиках инструмента

      # ======================================================
      # Debug / логирование
      # ======================================================

      debug: false                                 # Расширенные debug-логи
      debug_top: 10                                # Сколько элементов выводить в top/debug
      ema_log_enabled: false                       # Подробные EMA-логи

      # ======================================================
      # Backfill закрытых позиций
      # ======================================================

      backfill_run_on_startup: true                # Запускать backfill при старте
      backfill_enabled: true                       # Включить backfill

      backfill_interval_sec: 300                   # Интервал периодического backfill
      backfill_lookback_days: 3                    # Смотреть закрытые позиции за N дней назад
      backfill_batch_limit: 50                     # Размер батча backfill

      backfill_overwrite: true                     # Перезаписывать уже рассчитанные поля
      backfill_fill_fees: true                     # Подтягивать/заполнять fees
      backfill_diff_tol: 1e-9                      # Допуск сравнения при решении об обновлении

      backfill_drain_on_startup: true              # При старте прогнать backfill "до конца"
      backfill_max_startup_batches: 200            # Ограничение числа стартовых батчей
      backfill_log_empty: true                     # Логировать пустые проходы backfill
      backfill_log_each: false                     # Логировать каждую закрытую позицию

      # ======================================================
      # Portfolio cap
      # ======================================================

      # Если сумма value открытых позиций / wallet >= ratio,
      # новые сделки не открывать.
      # 0 = OFF
      portfolio_cap_ratio: 0.7                     # Максимальная суммарная загрузка портфеля относительно кошелька

      # ======================================================
      # Reconcile (сверка биржи и леджера)
      # ======================================================

      reconcile_enabled: true                      # Включить reconcile вообще
      reconcile_qty_tolerance: 1e-8                # Допуск по qty

      reconcile_auto_close_ledger: true            # Автоматически закрывать запись в леджере, если на бирже позиции нет
      reconcile_auto_adjust_ledger_qty_current: true # Автоматически выравнивать qty_current по бирже
      reconcile_adjust_only_our_positions: true    # Корректировать только позиции, созданные этим ботом
      reconcile_ignore_external_positions: true    # Игнорировать внешние позиции
      reconcile_every_n_cycles: 3                  # Делать reconcile не каждый цикл, а раз в N циклов
      reconcile_log_diffs: true                    # Логировать найденные расхождения
      reconcile_external_log_level: debug          # Уровень логирования внешних позиций

      reconcile_binance_adds_enabled: true         # Сверять исполнения ADDn по истории Binance
      reconcile_binance_adds_every_sec: 300        # Как часто делать такую сверку по истории Binance

      # Эти параметры специально ослаблены для снижения шума по value:
      # если qty совпадает, не надо каждый цикл дергать value_usdt
      reconcile_auto_adjust_ledger_value_usdt: true  # Автоматически обновлять ledger value_usdt при значимом расхождении
      reconcile_refresh_position_value_usdt: true    # Делать refresh value_usdt по текущему рынку во время reconcile
      reconcile_value_refresh_min_delta_usdt: 2.0    # Минимальное изменение value для обновления
      reconcile_value_tolerance_usdt: 2.5            # Порог, после которого value mismatch считается значимым

      # ======================================================
      # Auto recovery
      # ======================================================

      auto_recovery_enabled: true                  # Включить авто-восстановление защитных ордеров
      recovery_place_sl: true                      # Пытаться восстанавливать SL
      recovery_place_tp: true                      # Пытаться восстанавливать TP
      recovery_max_age_hours: 240                  # Не восстанавливать слишком старые позиции

      # ======================================================
      # Async REST
      # ======================================================

      async_rest_enabled: true                     # Включить параллельные REST-запросы
      async_rest_workers: 6                        # Количество воркеров
      async_rest_timeout_sec: 8                    # Таймаут async REST-запроса в секундах